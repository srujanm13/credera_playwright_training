// Create an object to store employee information
const employee = {
    name: "John",         
    age: 30,              
    department: "QA"      
};

// Create a formatted sentence using object properties
const sentence = `${employee.name} aged ${employee.age} works in ${employee.department}`;

// Print the formatted sentence to the console
console.log(sentence);